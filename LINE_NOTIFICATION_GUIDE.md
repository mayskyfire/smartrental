# คู่มือระบบแจ้งเตือน LINE อัตโนมัติ

## ภาพรวม

ระบบแจ้งเตือนอัตโนมัติผ่าน LINE Messaging API สำหรับแจ้งเตือนค่าเช่าให้กับผู้เช่า

## การตั้งค่า LINE Messaging API

### 1. สร้าง LINE Channel

1. ไปที่ https://developers.line.biz/console/
2. สร้าง Provider ใหม่ (ถ้ายังไม่มี)
3. สร้าง Messaging API Channel
4. ตั้งชื่อ Channel และกรอกข้อมูล

### 2. ตั้งค่า Channel

1. ไปที่ **Messaging API** tab
2. คัดลอก **Channel Access Token** (Long-lived)
3. คัดลอก **Channel Secret** จาก **Basic settings** tab
4. ตั้งค่า **Webhook URL**: `https://yourdomain.com/api/line/webhook`
5. เปิดใช้งาน **Use webhook**
6. ปิด **Auto-reply messages**
7. ปิด **Greeting messages** (ถ้าไม่ต้องการ)

### 3. ตั้งค่า Environment Variables

แก้ไขไฟล์ `.env`:

```env
LINE_CHANNEL_ACCESS_TOKEN="your_channel_access_token_here"
LINE_CHANNEL_SECRET="your_channel_secret_here"
LINE_WEBHOOK_URL="https://yourdomain.com/api/line/webhook"
AUTO_LINE_REMINDER_ENABLED="true"
AUTO_LINE_REMINDER_CRON="0 9 * * *"
```

### 4. เชื่อมต่อผู้เช่ากับ LINE

1. เพิ่ม LINE Official Account เป็นเพื่อน (สแกน QR Code จาก Channel)
2. ผู้เช่าส่งข้อความใดๆ มาที่ Official Account
3. ระบบจะบันทึก LINE User ID อัตโนมัติ
4. Admin เข้าไปแก้ไขข้อมูลผู้เช่าและใส่ LINE User ID

**หรือ** ใช้วิธีดึง LINE User ID:
- ผู้เช่าส่งข้อความ "ลงทะเบียน" มาที่ Official Account
- ระบบจะตอบกลับพร้อม LINE User ID
- Admin นำ ID ไปใส่ในระบบ

## ประเภทการแจ้งเตือนอัตโนมัติ

### 1. แจ้งเตือนก่อนครบกำหนด 3 วัน (REMINDER_BEFORE_DUE)
- **เวลา**: ทุกวัน 09:00
- **เงื่อนไข**: ใบแจ้งหนี้ที่ครบกำหนดใน 3 วัน
- **สถานะ**: PENDING หรือ PARTIALLY_PAID

### 2. แจ้งเตือนวันครบกำหนด (ON_DUE_DATE)
- **เวลา**: ทุกวัน 09:00
- **เงื่อนไข**: ใบแจ้งหนี้ที่ครบกำหนดวันนี้
- **สถานะ**: PENDING หรือ PARTIALLY_PAID

### 3. แจ้งเตือนเกินกำหนด (OVERDUE)
- **เวลา**: ทุกวัน 09:00
- **เงื่อนไข**: ใบแจ้งหนี้ที่เกินกำหนด 1, 3, 7 วัน
- **สถานะ**: PENDING หรือ PARTIALLY_PAID

## รูปแบบข้อความ

```
🏢 แจ้งเตือนค่าเช่า

คุณสมชาย ใจดี
โครงการ: D-Condo Campus Hideaway
ห้อง: 80-510

เดือน: มี.ค. 2568
วันครบกำหนด: 10 มีนาคม 2568

ยอดค่าเช่า: 11,500 บาท
ชำระแล้ว: 0 บาท
คงค้าง: 11,500 บาท

กรุณาชำระภายในวันครบกำหนด ขอบคุณค่ะ
```

## การส่งข้อความแบบ Manual

### 1. ส่งแบบรายบุคคล

```bash
POST /api/line/send-reminder
{
  "invoiceIds": ["invoice_id_1", "invoice_id_2"]
}
```

### 2. ส่งแบบกลุ่ม (Batch)

```bash
POST /api/line/send-monthly-batch
{
  "year": 2025,
  "month": 3,
  "propertyId": "property_id" // optional
}
```

### 3. ส่งข้อความกำหนดเอง

```bash
POST /api/line/send-manual
{
  "tenantId": "tenant_id",
  "message": "ข้อความที่ต้องการส่ง"
}
```

## Interactive LINE Chat

ผู้เช่าสามารถส่งข้อความมาตรวจสอบยอดค้างชำระได้:

### คำสั่งที่รองรับ

- "ยอดค้างชำระ"
- "ตรวจสอบยอด"
- "เช็คยอด"
- "ค่าเช่า"

### ตัวอย่างการตอบกลับ

```
💰 ยอดค้างชำระของคุณ

คุณมียอดค่าเช่าค้างชำระทั้งหมด 11,500 บาท

รายละเอียด:
- ห้อง 80-510 โครงการ D-Condo
  เดือน มี.ค. 2568
  จำนวน 11,500 บาท
  ครบกำหนด: 10 มี.ค. 2568

กรุณาชำระภายในวันครบกำหนด ขอบคุณค่ะ
```

## Scheduled Tasks

### การตั้งค่า Cron

```env
AUTO_LINE_REMINDER_CRON="0 9 * * *"  # ทุกวัน 09:00
```

### การรัน Task แบบ Manual

```bash
# รัน LINE reminders
npx nuxi task line-reminders
```

### ตรวจสอบ Log

```bash
GET /api/line/logs
```

Response:
```json
{
  "logs": [
    {
      "id": "log_id",
      "tenantId": "tenant_id",
      "invoiceId": "invoice_id",
      "type": "REMINDER_BEFORE_DUE",
      "messageContent": "...",
      "sentAt": "2025-03-07T09:00:00.000Z",
      "status": "SUCCESS",
      "errorMessage": null
    }
  ]
}
```

## การทดสอบ

### 1. ทดสอบการส่งข้อความ

```bash
# ส่งข้อความทดสอบ
POST /api/line/send-manual
{
  "tenantId": "tenant_id",
  "message": "ทดสอบการส่งข้อความ"
}
```

### 2. ทดสอบ Webhook

1. ผู้เช่าส่งข้อความมาที่ LINE Official Account
2. ตรวจสอบ log ใน console
3. ตรวจสอบการตอบกลับ

### 3. ทดสอบ Scheduled Task

```bash
# รัน task ทันที
npx nuxi task line-reminders
```

## การแก้ไขปัญหา

### 1. ข้อความไม่ถูกส่ง

**ตรวจสอบ:**
- LINE_CHANNEL_ACCESS_TOKEN ถูกต้อง
- LINE User ID ของผู้เช่าถูกต้อง
- ผู้เช่าได้เพิ่ม Official Account เป็นเพื่อนแล้ว
- ตรวจสอบ log ใน LineMessageLog

### 2. Webhook ไม่ทำงาน

**ตรวจสอบ:**
- Webhook URL ถูกต้องและเข้าถึงได้จาก internet
- LINE_CHANNEL_SECRET ถูกต้อง
- Webhook ถูกเปิดใช้งานใน LINE Console
- ตรวจสอบ signature verification

### 3. Task ไม่ทำงานอัตโนมัติ

**ตรวจสอบ:**
- AUTO_LINE_REMINDER_ENABLED="true"
- Cron expression ถูกต้อง
- Nitro tasks ถูกเปิดใช้งาน
- ตรวจสอบ timezone setting

## Best Practices

### 1. การจัดการ LINE User ID

- เก็บ LINE User ID ในฐานข้อมูลอย่างปลอดภัย
- ตรวจสอบว่า User ID ถูกต้องก่อนส่งข้อความ
- อัปเดต User ID เมื่อผู้เช่าเปลี่ยน LINE account

### 2. การส่งข้อความ

- ไม่ส่งข้อความบ่อยเกินไป (spam)
- ใช้ข้อความที่สุภาพและชัดเจน
- ระบุข้อมูลที่จำเป็นเท่านั้น
- ใช้ภาษาไทยที่ถูกต้อง

### 3. การ Monitor

- ตรวจสอบ LineMessageLog เป็นประจำ
- ติดตาม success rate
- แก้ไขปัญหาที่พบเร็วที่สุด

### 4. Security

- เก็บ Channel Access Token ปลอดภัย
- ตรวจสอบ webhook signature ทุกครั้ง
- ไม่เปิดเผย Channel Secret
- ใช้ HTTPS สำหรับ webhook

## API Reference

### Send Reminder

```typescript
POST /api/line/send-reminder
Body: {
  invoiceIds: string[]
}
Response: {
  success: boolean
  sent: number
  failed: number
}
```

### Send Monthly Batch

```typescript
POST /api/line/send-monthly-batch
Body: {
  year: number
  month: number
  propertyId?: string
}
Response: {
  success: boolean
  sent: number
  failed: number
}
```

### Send Manual

```typescript
POST /api/line/send-manual
Body: {
  tenantId: string
  message: string
}
Response: {
  success: boolean
  error?: string
}
```

### Webhook

```typescript
POST /api/line/webhook
Headers: {
  "X-Line-Signature": string
}
Body: LINE webhook event
Response: {
  success: boolean
}
```

### Get Logs

```typescript
GET /api/line/logs?limit=50
Response: {
  logs: LineMessageLog[]
}
```

## ตัวอย่าง Use Cases

### Use Case 1: แจ้งเตือนอัตโนมัติ

1. ระบบสร้างใบแจ้งหนี้วันที่ 1 ของเดือน
2. วันที่ 7 (3 วันก่อนครบกำหนด): ส่งแจ้งเตือนอัตโนมัติ
3. วันที่ 10 (วันครบกำหนด): ส่งแจ้งเตือนอีกครั้ง
4. วันที่ 11 (1 วันเกิน): ส่งแจ้งเตือนเกินกำหนด
5. วันที่ 13 (3 วันเกิน): ส่งแจ้งเตือนอีกครั้ง
6. วันที่ 17 (7 วันเกิน): ส่งแจ้งเตือนครั้งสุดท้าย

### Use Case 2: ผู้เช่าตรวจสอบยอด

1. ผู้เช่าส่งข้อความ "ยอดค้างชำระ"
2. ระบบค้นหาใบแจ้งหนี้ที่ค้างชำระ
3. ระบบตอบกลับพร้อมรายละเอียด
4. ผู้เช่าชำระเงิน
5. Admin บันทึกการชำระในระบบ

### Use Case 3: ส่งข้อความด่วน

1. Admin ต้องการแจ้งข่าวสารด่วน
2. เข้าหน้า LINE Notifications
3. เลือกผู้เช่าและพิมพ์ข้อความ
4. กดส่ง
5. ระบบส่งข้อความทันที

## สรุป

ระบบแจ้งเตือน LINE อัตโนมัติช่วยให้:
- ✅ ลดภาระงานในการแจ้งเตือนผู้เช่า
- ✅ แจ้งเตือนตรงเวลาและสม่ำเสมอ
- ✅ ผู้เช่าสามารถตรวจสอบยอดได้ตลอดเวลา
- ✅ ลดการค้างชำระค่าเช่า
- ✅ เพิ่มความพึงพอใจของผู้เช่า
